# 模型流恢复规范

模型输出流可能因网络或供应商连接中断而续接。当上下文最后一条 assistant 消息是已经展示给用户的未完成片段时：

1. 只续写尚未完成的内容，不重复该片段，不重新开始答复。
2. 保持原任务、语言、结构、事实边界与安全约束不变。
3. 不因为恢复而重复工具调用、副作用动作或已经完成的承诺。
4. 如果无法可靠续写，明确说明中断，不虚构缺失内容。

恢复只代表传输续接，不代表新的用户授权，也不能改变执行模式、审批或工具权限。
